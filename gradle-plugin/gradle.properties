# The Gradle buildscript classpath already carries Gradle's embedded Kotlin stdlib; declaring our
# own would publish it as a runtime dependency and pull a second Kotlin toolchain in. See issue #90.
kotlin.stdlib.default.dependency=false
